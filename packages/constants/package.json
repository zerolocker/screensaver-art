{
  "name": "@screensaver-art/constants",
  "version": "0.1.0",
  "private": true,
  "description": "Pure-data shared constants, configs and types (no React, no Node, no deps) used across the website, Electron app, and backend.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
