node_modules/
out/
dist/
# Developer-specific signing/notary settings for `pnpm dist:mac:release`.
# Not secret (see release.env.example), but per-developer — keep it untracked.
release.env
# Built screensaver artifacts — regenerated by scripts/bundle-appex.sh.
resources/ScreensaverArtExtension.appex/
resources/lart-screensaver-helper
electron.vite.config.*.mjs
